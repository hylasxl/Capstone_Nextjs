import DefaultLayout from "@/components/layout/DefaultLayout";

export default function DefaultMainLayout({ children }: { children: React.ReactNode }) {
    return <DefaultLayout>{children}</DefaultLayout>;
}